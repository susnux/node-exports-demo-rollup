// run `npm run exec:dev` in the terminal to run the original file
// run `npm run exec:build` in the terminal to run the built file

import { foo } from 'dummy-exported/foo.js';

console.log(`foo === ${foo}`);
